# Architecture & Project Structure

- Organize files logically by feature or domain, rather than by technical role (e.g., group all "User" related files together, rather than having all "Controllers" together and all "Models" together).
- Keep dependencies between modules explicit and minimized.
- Separate business logic from framework-specific code (I/O, network, database).
