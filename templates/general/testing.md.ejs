# Testing Guidelines

- Write tests for all critical business logic.
- Follow the AAA pattern (Arrange, Act, Assert).
- Aim for high test coverage on core algorithms, but prioritize meaningful assertions over pure coverage percentages.
- Isolate unit tests by mocking external boundaries (API calls, database queries, file system).
